// /* eslint-disable react/prop-types */
// /* eslint-disable no-unused-vars */
function Button({ style }) {
    const base =
      "";
  
    return (
      <a href="#"  className={`${base + " " + style}`}>
        Get Started
      </a>
    );
  }
  
  export default Button;
  